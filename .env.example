# Copy to .env and fill in.

# Shared secret required on every /api/* request (sent as the X-API-Key
# header or a ?key= query parameter). Generate a long random value, e.g.:
#   openssl rand -hex 32
API_KEY=
